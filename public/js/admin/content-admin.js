(() => {
  'use strict';
  const q=selector=>document.querySelector(selector),state={client:null,session:null,role:null,items:[],audit:[],view:'list',editing:null};
  const uuid=()=>crypto.randomUUID();
  const message=(target,text,error=false)=>{const el=q(target);if(el){el.textContent=text;el.style.color=error?'#ff8291':'';}};
  const rpc=async(name,args={})=>{if(!state.client)throw Error('client_unavailable');const{data,error}=await state.client.rpc(name,args);if(error)throw Object.assign(Error(error.code||'request_rejected'),{code:error.code});return data;};
  const allowed=(...roles)=>roles.includes(state.role);
  function isoInput(value){if(!value)return'';const date=new Date(value);return new Date(date.getTime()-date.getTimezoneOffset()*60000).toISOString().slice(0,16);}
  function parsePayload(){let payload;try{payload=JSON.parse(q('#contentPayload').value||'{}')}catch(_){throw Error('Payload geçerli JSON olmalı.')}if(!payload||Array.isArray(payload)||typeof payload!=='object')throw Error('Payload JSON nesnesi olmalı.');const text=JSON.stringify(payload);if(/"(answer|solution|correct_answer|combo_order|cipher_answer)"\s*:/i.test(text))throw Error('Doğru cevap public payload içinde bulunamaz.');return payload;}
  function documentFromForm(){const rewardType=q('#rewardType').value,rewardAmount=Number(q('#rewardAmount').value||0);return{content_type:q('#contentType').value,status:q('#contentStatus').value,title_tr:q('#titleTr').value.trim(),title_en:q('#titleEn').value.trim(),description_tr:q('#descriptionTr').value.trim(),description_en:q('#descriptionEn').value.trim(),starts_at:new Date(q('#startsAt').value).toISOString(),ends_at:new Date(q('#endsAt').value).toISOString(),reward_type:rewardType,reward_amount:rewardType==='none'?0:rewardAmount,answer_normalization:q('#answerNormalization').value,payload:parsePayload()};}
  function setWorkspace(enabled){q('#adminGate').hidden=enabled;q('#adminWorkspace').hidden=!enabled;q('#adminSignOut').hidden=!enabled;}
  function renderStats(){const attempts=state.items.reduce((sum,item)=>sum+Number(item.attempt_count||0),0),correct=state.items.reduce((sum,item)=>sum+Number(item.correct_count||0),0),claims=state.items.reduce((sum,item)=>sum+Number(item.claim_count||0),0);q('#statContent').textContent=state.items.length;q('#statAttempts').textContent=attempts;q('#statSuccess').textContent=attempts?`${Math.round(correct/attempts*100)}%`:'0%';q('#statClaims').textContent=claims;}
  function card(item){const edit=allowed('editor','publisher','super_admin');return`<article class="ops-card"><div><span class="ops-status ${item.status}">${item.status.toUpperCase()}</span><h3>${escapeHtml(item.title_tr)}</h3><p>${escapeHtml(item.content_type)} • ${new Date(item.starts_at).toLocaleString('tr-TR')} → ${new Date(item.ends_at).toLocaleString('tr-TR')}</p><small>${item.reward_type}: ${item.reward_amount} • v${item.version} • ${item.attempt_count||0} deneme • ${item.claim_count||0} claim</small></div><div class="ops-card-actions"><button type="button" data-edit-content="${item.id}" ${edit?'':'disabled'}>Düzenle</button><button type="button" data-copy-content="${item.id}" ${edit?'':'disabled'}>Kopyala</button></div></article>`;}
  function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));}
  function renderList(){q('#adminList').innerHTML=state.items.length?state.items.map(card).join(''):'<article class="ops-card">Henüz içerik yok.</article>';}
  function renderCalendar(){const groups=new Map();for(const item of state.items){const key=new Date(item.starts_at).toISOString().slice(0,10);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(item);}q('#adminCalendar').innerHTML=[...groups.entries()].slice(0,35).map(([day,items])=>`<article class="ops-day"><b>${day}</b>${items.map(item=>`<button type="button" data-edit-content="${item.id}">${escapeHtml(item.title_tr)}<small>${item.status}</small></button>`).join('')}</article>`).join('')||'<article class="ops-day">Plan yok</article>';}
  function renderAudit(){q('#adminAudit').innerHTML=state.audit.map(item=>`<article class="ops-card"><div><span class="ops-status">${escapeHtml(item.action)}</span><h3>${escapeHtml(item.entity_type)}</h3><small>${new Date(item.created_at).toLocaleString('tr-TR')} • ${escapeHtml(item.entity_id||'')}</small></div></article>`).join('')||'<article class="ops-card">Audit kaydı yok.</article>';}
  function switchView(view){state.view=view;for(const id of ['list','calendar','audit']){q(`#admin${id[0].toUpperCase()+id.slice(1)}`).hidden=id!==view;}document.querySelectorAll('[data-admin-view]').forEach(button=>button.classList.toggle('active',button.dataset.adminView===view));if(view==='audit')loadAudit();}
  async function load(){const result=await rpc('admin_get_daily_content');state.role=result.role;state.items=result.items||[];q('#adminRole').textContent=state.role.replace('_',' ').toUpperCase();setWorkspace(true);renderStats();renderList();renderCalendar();}
  async function loadAudit(){try{state.audit=await rpc('admin_get_content_audit',{p_limit:100})||[];renderAudit()}catch(_){q('#adminAudit').innerHTML='<article class="ops-card">Audit okunamadı.</article>';}}
  function openEditor(item=null,copy=false){if(!allowed('editor','publisher','super_admin'))return;state.editing=copy?null:item;q('#contentEditor').hidden=false;q('#contentEditorTitle').textContent=item?(copy?'İçeriği kopyala':'İçeriği düzenle'):'Yeni içerik';q('#contentId').value=copy?'':item?.id||'';q('#contentVersion').value=copy?'':item?.version||'';q('#contentType').value=item?.content_type||'daily_combo';q('#contentStatus').value=['draft','scheduled'].includes(item?.status)?item.status:'draft';q('#titleTr').value=item?.title_tr||'';q('#titleEn').value=item?.title_en||'';q('#descriptionTr').value=item?.description_tr||'';q('#descriptionEn').value=item?.description_en||'';q('#startsAt').value=isoInput(item?.starts_at||new Date(Date.now()+3600000));q('#endsAt').value=isoInput(item?.ends_at||new Date(Date.now()+90000000));q('#rewardType').value=item?.reward_type||'none';q('#rewardAmount').value=item?.reward_amount||0;q('#answerNormalization').value=item?.answer_normalization||'nfkc_lower_trim';q('#contentAnswer').value='';q('#contentPayload').value=JSON.stringify(item?.payload||{},null,2);q('[data-editor-publish]').disabled=!allowed('publisher','super_admin')||!item;q('[data-editor-cancel]').disabled=!allowed('publisher','super_admin')||!item;message('#editorMessage','');}
  function closeEditor(){q('#contentEditor').hidden=true;state.editing=null;}
  async function save(event){event.preventDefault();try{const data=await rpc('admin_upsert_daily_content',{p_content_id:q('#contentId').value||null,p_document:documentFromForm(),p_answer:q('#contentAnswer').value||null,p_expected_version:Number(q('#contentVersion').value)||null,p_request_id:uuid()});message('#editorMessage',`Kaydedildi • v${data.version}`);await load();openEditor(state.items.find(item=>item.id===data.id)||data)}catch(error){message('#editorMessage',error.message,true);}}
  async function publish(){if(!state.editing)return;try{await rpc('admin_publish_daily_content',{p_content_id:state.editing.id,p_expected_version:state.editing.version,p_request_id:uuid()});closeEditor();await load()}catch(error){message('#editorMessage',error.message,true);}}
  async function cancel(){if(!state.editing)return;try{await rpc('admin_cancel_daily_content',{p_content_id:state.editing.id,p_expected_version:state.editing.version,p_request_id:uuid()});closeEditor();await load()}catch(error){message('#editorMessage',error.message,true);}}
  function bind(){q('#adminLoginForm').addEventListener('submit',async event=>{event.preventDefault();try{const{error}=await state.client.auth.signInWithOtp({email:q('#adminEmail').value,options:{emailRedirectTo:location.href.split('#')[0]}});if(error)throw error;message('#adminGateMessage','Giriş bağlantısı gönderildi.')}catch(_){message('#adminGateMessage','Giriş başlatılamadı.',true);}});q('#adminSignOut').addEventListener('click',()=>state.client.auth.signOut());q('#adminNewContent').addEventListener('click',()=>openEditor());q('#contentForm').addEventListener('submit',save);document.addEventListener('click',event=>{const edit=event.target.closest('[data-edit-content]');if(edit)return openEditor(state.items.find(item=>item.id===edit.dataset.editContent));const copy=event.target.closest('[data-copy-content]');if(copy)return openEditor(state.items.find(item=>item.id===copy.dataset.copyContent),true);if(event.target.closest('[data-editor-close]'))return closeEditor();if(event.target.closest('[data-editor-copy]'))return openEditor(state.editing,true);if(event.target.closest('[data-editor-preview]'))return message('#editorMessage','Önizleme: '+documentFromForm().title_tr);if(event.target.closest('[data-editor-publish]'))return publish();if(event.target.closest('[data-editor-cancel]'))return cancel();const view=event.target.closest('[data-admin-view]');if(view)switchView(view.dataset.adminView);});}
  async function init(){const result=window.MarinoRuntimeConfig?.read(window.MARINO_CONFIG);if(!result?.ok||result.config.targetEnvironment!=='staging'){message('#adminGateMessage','STAGING runtime yapılandırması doğrulanamadı.',true);return}state.client=window.supabase?.createClient(result.config.supabaseUrl,result.config.supabasePublishableKey,{auth:{persistSession:false,autoRefreshToken:true,detectSessionInUrl:true}});if(!state.client){message('#adminGateMessage','Güvenli istemci başlatılamadı.',true);return}bind();state.client.auth.onAuthStateChange(async(event,session)=>{state.session=session;if(event==='SIGNED_OUT'||!session){state.role=null;setWorkspace(false);q('#adminRole').textContent='Oturum gerekli';return}try{await load()}catch(error){setWorkspace(false);message('#adminGateMessage',error.code==='42501'?'Bu hesap için içerik yönetimi yetkisi yok.':'Yetki doğrulanamadı.',true);}});const{data}=await state.client.auth.getSession();if(data.session){state.session=data.session;try{await load()}catch(_){message('#adminGateMessage','İçerik yönetimi yetkisi yok.',true);}}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
