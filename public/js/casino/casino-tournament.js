(() => {
  'use strict';

  const MOCK_PLAYERS=['Luna Vale','Arda Neon','Mila Crown','Deniz Noir','Leo Marina','Ada Royale','Eren Gold','Nora Vega','Mert Azure','Lina Pearl','Can Riviera','Maya Vento','Bora Crest','Elif Nova','Emir Deck','Sena Lux','Atlas Blue','Duru Ace','Kaan Porto','İpek Halo'].map((name,index)=>({id:`mock-${index+1}`,name,score:285-index*11+(index%3)*5,games:['slots',...(index%2?['roulette']:[]),...(index%3?['blackjack']:[])],league:['Elmas','Platin','Altın','Gümüş'][index%4]}));
  const SCORE_EVENTS=Object.freeze({mission_claim:12,chain_complete:35,triple_visit:25});
  function tournamentPoints(event){return SCORE_EVENTS[event]||0;}
  function createTournamentState(){return{score:0,games:new Set(),startedAt:Date.now()};}
  function applyTournamentEvent(state,event,payload={}){if(event==='casino_visit'&&payload.game)state.games.add(payload.game);const points=tournamentPoints(event);state.score+=points;return points;}
  function leaderboard(state,name='Sen'){return[...MOCK_PLAYERS,{id:'local-player',name,score:state.score,games:[...state.games],league:'Marino'}].sort((a,b)=>b.score-a.score).map((player,index)=>({...player,rank:index+1}));}

  const preview=()=>window.MarinoLocalPreview?.detect?.(window.location,window.Telegram?.WebApp)===true;
  const state=createTournamentState();let overlay=null;
  const initials=name=>name.split(' ').map(word=>word[0]).join('').slice(0,2).toUpperCase();
  function rows(){return leaderboard(state).map(player=>`<li class="${player.id==='local-player'?'is-player':''} ${player.rank<=3?'top-'+player.rank:''}"><b class="tournament-rank">${player.rank}</b><i>${initials(player.name)}</i><span><strong>${player.name}</strong><small>${player.league}</small></span><div class="tournament-games"><em class="${player.games.includes('slots')?'on':''}">S</em><em class="${player.games.includes('roulette')?'on':''}">R</em><em class="${player.games.includes('blackjack')?'on':''}">B</em></div><b>${player.score} TP</b></li>`).join('');}
  function summaryMarkup(){const rank=leaderboard(state).find(player=>player.id==='local-player')?.rank||21;return`<button type="button" data-casino-tournament-open><span>HAFTALIK TURNUVA</span><strong>#${rank} Casino Cup</strong><small>${preview()?state.score+' demo TP':'Salt okunur demo'}</small></button>`;}
  function ensure(){if(overlay)return overlay;overlay=document.createElement('div');overlay.className='casino-tournament-overlay';overlay.hidden=true;overlay.innerHTML='<button type="button" class="casino-tournament-backdrop" aria-label="Turnuvayı kapat"></button><section class="casino-tournament-sheet" role="dialog" aria-modal="true" aria-label="Marino Weekly Casino Cup"><header><div><span>DEMO ETKİNLİK</span><h3>MARINO WEEKLY CASINO CUP</h3></div><button type="button" data-casino-tournament-close aria-label="Kapat">×</button></header><div class="casino-tournament-body"></div></section>';document.body.append(overlay);return overlay;}
  function render(){const body=ensure().querySelector('.casino-tournament-body'),minutes=Math.floor((Date.now()-state.startedAt)/60000);body.innerHTML=`<section class="tournament-hero"><span>SANAL • NAKİT DEĞERİ YOKTUR</span><strong>${preview()?state.score:'—'} TP</strong><p>Görev ve zincir tamamlayarak demo turnuva puanı kazan. Bahis miktarı, kazanç veya kayıp skoru etkilemez.</p><small>Oturum: ${minutes} dk • 20 dakikada mola önerilir</small></section><ol class="casino-leaderboard">${rows()}</ol><footer>20 güvenli mock oyuncu • ağ, RPC veya kalıcı sonuç kullanılmaz.</footer>`;}
  function open(){ensure().hidden=false;document.body.classList.add('casino-progression-open');render();window.MarinoAudio?.play?.('ui_open');}
  function close(){if(!overlay||overlay.hidden)return;overlay.hidden=true;document.body.classList.remove('casino-progression-open');window.MarinoAudio?.play?.('ui_close');}
  function bind(){document.addEventListener('marino:casino-event',event=>{if(!event.detail?.preview||!preview())return;applyTournamentEvent(state,event.detail.event,event.detail.payload);if(overlay&&!overlay.hidden)render();});document.addEventListener('click',event=>{if(event.target.closest('[data-casino-tournament-open]'))return open();if(event.target.closest('[data-casino-tournament-close],.casino-tournament-backdrop'))return close();});document.addEventListener('keydown',event=>{if(event.key==='Escape'&&overlay&&!overlay.hidden)close();});}
  if(typeof window!=='undefined'){window.MarinoCasinoTournament=Object.freeze({summaryMarkup,open,close,snapshot:()=>({score:state.score,games:[...state.games],rank:leaderboard(state).find(item=>item.id==='local-player')?.rank,preview:preview()})});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();}
  if(typeof module!=='undefined')module.exports={MOCK_PLAYERS,SCORE_EVENTS,createTournamentState,tournamentPoints,applyTournamentEvent,leaderboard};
})();
