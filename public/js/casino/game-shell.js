(() => {
  'use strict';
  const state={game:null,active:false,startedAt:0,timer:0,backBound:false};
  const icon=(path,label)=>`<svg viewBox="0 0 24 24" aria-label="${label}" role="img"><path d="${path}"/></svg>`;
  function ensure(){let root=document.querySelector('#casinoGameShell');if(root)return root;root=document.createElement('section');root.id='casinoGameShell';root.className='casino-game-shell';root.hidden=true;root.setAttribute('aria-modal','true');root.setAttribute('role','dialog');root.innerHTML=`<div class="casino-shell-backdrop"></div><div class="casino-shell-frame"><header class="casino-shell-header"><div class="casino-shell-brand"><span>MARINO CASINO</span><h2 id="casinoShellTitle">Demo oyun</h2></div><div class="casino-shell-wallet"><span>DEMO JETON</span><strong id="casinoShellBalance">0</strong></div><button type="button" data-shell-action="sound" aria-label="Sesi aç veya kapat">${icon('M5 9v6h4l5 4V5L9 9H5m12 1a4 4 0 0 1 0 4','Ses')}</button><button type="button" data-shell-action="fullscreen" aria-label="Tam ekran">${icon('M8 3H3v5m13-5h5v5M8 21H3v-5m13 5h5v-5','Tam ekran')}</button><button type="button" data-shell-action="close" aria-label="Oyundan çık">${icon('M6 6l12 12M18 6 6 18','Kapat')}</button></header><div class="casino-shell-meta"><span id="casinoShellRights">0 Free Spin • 0 Free Bet</span><button type="button" data-shell-action="rules">Kurallar</button><button type="button" data-shell-action="history">Geçmiş</button><span id="casinoShellSession">00:00</span></div><main id="casinoShellStage" class="casino-shell-stage"></main><aside id="casinoShellPanel" class="casino-shell-panel" hidden></aside><footer><span>Demo sonucu, nakit değeri yoktur</span><button type="button" data-shell-action="break">Mola ver</button></footer></div>`;document.body.appendChild(root);
    root.addEventListener('pointerdown',event=>{event.stopPropagation()});
    root.addEventListener('click',onAction);return root;}
  function wallet(){const snap=window.MarinoDemoWallet?.snapshot?.()||{balance:0,freeSpins:0,freeBets:0};document.querySelector('#casinoShellBalance').textContent=new Intl.NumberFormat('tr-TR').format(snap.balance);document.querySelector('#casinoShellRights').textContent=`${snap.freeSpins} Free Spin • ${snap.freeBets} Free Bet`;}
  function onAction(event){const action=event.target.closest('[data-shell-action]')?.dataset.shellAction;if(!action)return;if(action==='close')return close();if(action==='rules')return panel('rules');if(action==='history')return panel('history');if(action==='break')return close(true);if(action==='fullscreen')return document.documentElement.requestFullscreen?.().catch(()=>{});if(action==='sound'){const button=event.target.closest('button');button.classList.toggle('muted');document.querySelector('#swSfx')?.classList.toggle('on',!button.classList.contains('muted'));}}
  function panel(type){const el=document.querySelector('#casinoShellPanel');if(!el)return;const history=state.game?.history?.()||[];el.hidden=false;el.innerHTML=`<button type="button" data-panel-close aria-label="Paneli kapat">×</button>${type==='rules'?`<h3>${state.game?.name||'Oyun'} Kuralları</h3>${state.game?.rules||''}`:`<h3>Sonuç Geçmişi</h3><ol>${history.length?history.map(item=>`<li>${item}</li>`).join(''):'<li>Henüz sonuç yok.</li>'}</ol>`}`;el.querySelector('[data-panel-close]').onclick=()=>{el.hidden=true};window.MarinoAudio?.play?.('ui_open');}
  function bindBack(){const back=window.Telegram?.WebApp?.BackButton;if(!back||state.backBound)return;back.onClick(close);back.show();state.backBound=true;}
  function unbindBack(){const back=window.Telegram?.WebApp?.BackButton;if(!back||!state.backBound)return;back.offClick?.(close);back.hide();state.backBound=false;}
  function formatTime(){const seconds=Math.floor((Date.now()-state.startedAt)/1000);return `${String(Math.floor(seconds/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`;}
  function open(game){const root=ensure();state.game=game;state.active=true;state.startedAt=Date.now();root.hidden=false;document.documentElement.classList.add('casino-game-open');document.body.classList.add('casino-scroll-lock');document.querySelector('#casinoShellTitle').textContent=game.name;const stage=document.querySelector('#casinoShellStage');stage.innerHTML='';stage.appendChild(game.mount());wallet();bindBack();clearInterval(state.timer);state.timer=setInterval(()=>{const el=document.querySelector('#casinoShellSession');if(el)el.textContent=formatTime();wallet();},1000);window.MarinoAudio?.play?.('ui_open');game.onOpen?.();}
  function close(force=false){if(!state.active)return true;if(!force&&state.game?.isActive?.()&&!window.confirm('Aktif tur devam ediyor. Oyundan çıkılsın mı?'))return false;state.game?.onClose?.();state.active=false;clearInterval(state.timer);unBindPanel();unbindBack();const root=ensure();root.hidden=true;document.documentElement.classList.remove('casino-game-open');document.body.classList.remove('casino-scroll-lock');window.MarinoAudio?.play?.('ui_close');return true;}
  function unBindPanel(){const panelEl=document.querySelector('#casinoShellPanel');if(panelEl)panelEl.hidden=true;}
  function keydown(event){if(state.active&&event.key==='Escape'){event.preventDefault();if(!document.querySelector('#casinoShellPanel')?.hidden)return unBindPanel();close();}}
  document.addEventListener('keydown',keydown,true);document.addEventListener('visibilitychange',()=>{if(state.active)state.game?.setPaused?.(document.hidden)});
  window.MarinoGameShell=Object.freeze({open,close,refreshWallet:wallet,isOpen:()=>state.active});
})();
